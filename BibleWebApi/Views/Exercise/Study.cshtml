@using BibleCore.Service.Data
@using BibleWebApi.Models
@model ExerciseModel
@{
    ViewData["Title"] = "Study";
}
<h2>Study - @Model.Name / @Model.WordListDescription (<a asp-controller="Exercise" asp-action="Start" asp-route-name="@Model.Name" asp-route-wordListId="@Model.WordListId" asp-route-range="@Model.Range">Practice</a>)</h2>
@foreach (var question in Model.Questions)
{
    <hr class="p-0 m-0" />
    <div class="row align-items-center" id="word-@question.Sequence">
        <div class="col serif">
            @question.Question
        </div>
        <div class="col serif">
            @foreach (var answer in question.Answers)
            {
                if (answer.IsCorrect)
                {
                    @answer.Answer
                }
            }
        </div>
        <div class="col serif">
            @{
                bool isFirst = true;
                foreach (var detail in question.Detail)
                {
                    if (!string.IsNullOrEmpty(detail))
                    {
                        if (isFirst)
                        {
                            isFirst = false;
                        }
                        else
                        {
                            <br />
                        }
                        @detail
                    }
                }
            }
        </div>
    </div>
}